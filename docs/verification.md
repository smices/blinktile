# 验收记录

截至 2026-09-24。这里只列当前版本的结果；软件模拟、固件编译、烧录、串口读回和肉眼观察是不同证据，不互相替代。逐项实物检查见[真机验收表](hardware.md)。

## 已完成

| 范围 | 结果 |
| --- | --- |
| `bash tools/test.sh` | 2026-09-24 通过：46 个图标、95 个字形、58 个 GIF 解码；网页控制器和 WebSocket 模拟器；协议与配网页边界；250 组原生固件 C++ 与网页渲染对照、5 个非法文本拒绝；竖线雨、桌宠节奏、临时命令恢复及 BOOT 按键授权的软件逻辑。 |
| `codex-status/.venv/bin/python codex-status/test_codex_status.py` | 2026-09-24 通过 7 项：hook 脱敏、会话／子代理状态、其他应用占用保护、串口分帧、模拟 BLE 单次请求写入／通知组帧与设备选择，以及无密钥授权重试、超时和原有密钥路径。 |
| `bash tools/build.sh` | 待烧录的新 ESP32-C3 固件使用 `huge_app` 分区编译通过：程序 1,368,245 字节（43%），静态 RAM 51,712 字节（15%）。 |
| 烧录与串口 | 2026-09-23 版本已烧录至连接的 ESP32-C3，写入哈希验证通过。重启后 `get` 读回桌宠模式、默认亮度 64/255；再发送亮度指令，读回 8/255（约 3%，重启后恢复默认）。此前串口实测临时 `loading` 到期后返回桌宠。2026-09-24 的 BOOT 授权改动尚未烧录。 |
| Codex 桥接 BLE 真机 | 本机 sample 环境已安装 Bleak；使用设备已有 `control_token` 完成实际 GATT 认证及 `get`，随后 BLE 桥接在 `127.0.0.1:8766` 接收合成 `PreToolUse`／`Stop` hook（HTTP 204）。串口 `get` 确认 BLE 已连接、`loading` 内容由桥接显示、亮度 8/255。这验证密钥认证与显示链路；当前会话仍有其他状态事件，不能由合成 `Stop` 单独证明自动回到桌宠，也不证明 Codex 客户端 hook 已信任并实际触发。 |

早期实物曾确认红绿蓝、四方向图标和逐行线性映射可辨；这不是新版竖线雨的肉眼验收。网页预览素材已重新生成并检查，但 GIF 与屏幕预览也不能替代灯板观感。

## 尚未完成

- 新版竖线雨的真机肉眼密度、速度、明暗评价，以及其他动画完整周期的实物验收。
- BLE 无密钥 BOOT 授权、网页 BLE、20 字节分段请求及异常断线后的恢复。实际真机发现分段认证请求可触发 `ble_request_timeout`／`invalid_json`；sample 改为单次 GATT 写入后密钥认证通过。通知拼包有模拟测试，尚未单独做真机边界测试。
- Wi-Fi 换网、错误密码、隐藏 SSID、DHCP 失败、候选网络期间断电和热点关闭的实物联测；现有配网页测试仅模拟 HTTP 响应，不会切换电脑网络。
- 电流表、温升、BLE/Wi-Fi 并发、WebSocket 真机通信，以及 Codex CLI／桌面端 hooks 信任后的自动状态显示。

电流字段是软件估算，不是测量值；未执行的真机项目保持未勾选。

## 2026-09-24 BLE 物理授权（待实物验收）

新代码允许设备运行后按住 BOOT 键至少 1 秒，对当前 BLE 会话执行无密钥 `auth`；一次长按只授权一次，松开才重新启用。不会通过 BLE 返回长期控制密钥，WebSocket 仍要求密钥。网页 BLE 连接与 codex-status 桥接均支持有限时间重试。原生固件传输测试覆盖按键持续时间、释放后拒绝、仅 BLE 可无密钥授权及一次长按不重复授权；桥接模拟测试覆盖重试、超时和原有密钥路径。`bash tools/test.sh` 与 `bash tools/build.sh` 已通过。

用户暂时不方便按实体 BOOT 键，因此**尚未烧录支持 BOOT 授权的新版固件**。现有固件的控制密钥认证已完成 BLE 真机连接与状态显示验收；按键认证仍不能标记为通过。设备运行亮度仍为 8/255，重启后恢复默认 64/255。
