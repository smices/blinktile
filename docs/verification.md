# 本地验收记录

验收日期：2026-09-22。以下结果区分软件检查与实物验证。

## 已执行

| 检查 | 结果 |
| --- | --- |
| `python3 tests/test_assets.py` | 46 个图标、95 个 ASCII 字形、58 个 GIF 解码与动画检查通过 |
| `node tests/test_engine.cjs` | 参数校验、1% 默认亮度、TTL、暂停、续期、桌宠恢复、滚动、速度与限流通过 |
| `node tests/test_client.cjs` | 旧连接回调隔离、连接取消、BLE 分包隔离与模拟器标记通过 |
| `python3 tests/test_transport.py` | 实际 JS 客户端连接本地 WebSocket 模拟器，认证、指令、错误与长度边界通过 |
| `python3 tests/test_firmware_render.py` | 从当前固件提取真实 C++ 渲染函数，250 组输出与 JS 对比通过；允许单通道 1 级舍入差；5 个非法文本用例被拒绝 |
| `python3 tests/test_setup_page.py` | 执行固件内嵌页面脚本，扫描、SSID 安全文本、提交和成功/失败/保存失败提示通过 |
| `python3 tests/test_firmware_transport.py` | 编译执行真实 Wi-Fi/BLE 辅助函数：联网超过 30 秒不误开 AP，失网满 30 秒回退，旧会话无法认证 |
| `bash tools/build.sh` | ESP32-C3 / Arduino-ESP32 3.3.11 / huge_app 编译通过；程序 1,351,055 字节（42%），静态 RAM 51,464 字节（15%） |

构建生成应用、引导程序、分区表和 4MB 合并 BIN。四个文件均未检出本机用户目录路径；发布包排除 ELF、map 和构建缓存。编译成功不等于实际运行内存或无线吞吐已通过验收。

浏览器中完成控制器认证、发送 `100%`、组合等待动画/彩虹/呼吸、GIF 播放与停止。全部 58 张图像成功加载；`file://` 离线页面可读取素材和启动预览。390px 视口检查无横向溢出。

已实际查看像素图鉴和动画帧条 PNG。浏览器截图接口两次超时，因此没有把 DOM 检查冒充完整页面截图验收。GIF 解码与帧检查不代表已观看每个动画完整周期。

审查发现并修复了时钟基准混用、旧连接影响新连接、过期状态恢复时间依赖轮询、鉴权类型错误等问题；对应回归检查已保留。Astra 最后定向复核确认：正常联网误开热点、BLE 重连认证/响应串会话、内嵌 NUL 绕过校验三项问题均已修复。编译还发现并修复了 BLE 栈接口不兼容；当前目标使用 NimBLE，连接定向通知分支已再次审查并编译通过。

## 实物验收

ESP32-C3 已通过 USB-Serial/JTAG 枚举并完成烧录，所有写入数据哈希验证通过。真机启动稳定；串口确认默认亮度为 3/255、配置热点已启动、错误命令不改变当前状态、暂停期间 TTL 仍到期、临时状态退出恢复桌宠。实物确认红绿蓝颜色、四个方向箭头、笑脸和逐行线性映射清晰可辨；最终状态为桌宠模式。

尚未完成电流表测量、温升、BLE 无线发现/20 字节分包、WebSocket 真机通信、Wi-Fi 换网、错误密码、隐藏 SSID、DHCP 失败、候选网络期间断电以及热点关闭验收。

原生 C++ 比对只替换灯珠输出边界，不覆盖 ESP32 调度与无线栈。配网页测试使用模拟 HTTP 响应，不证明真实 AP、DHCP 或 NVS 行为。电流是软件估算，必须以 [真机验收表](hardware.md) 完成实测。
